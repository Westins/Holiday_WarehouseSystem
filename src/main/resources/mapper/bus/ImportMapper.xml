<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.sw.bus.dao.ImportMapper">

    <!-- 查询每月进货数量 -->
    <select id="loadImportByMonth" resultType="Integer">
     SELECT
      SUM(number) AS number,
      DATE_FORMAT(importTime,'%Y%m') months
      FROM
      bus_import
      GROUP BY
      months
    </select>

    <!-- 查询今日进货数量 -->
    <select id="loadImportByNow" resultType="Integer">
        SELECT
        IFNULL(SUM(number),0) AS number
        FROM bus_import
        WHERE
        TO_DAYS(importTime) = TO_DAYS(NOW())
    </select>
</mapper>
